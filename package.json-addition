{
  "scripts": {
    "optimize-images": "ts-node scripts/optimize-images.ts",
    "build": "npm run optimize-images && next build"
  },
  "devDependencies": {
    "sharp": "^0.32.1",
    "ts-node": "^10.9.1"
  }
}
